# Single pose landmark in normalized image coordinates (MediaPipe convention).
# x, y are typically in [0, 1] relative to image width/height; z is relative depth.
uint32 index
float32 x
float32 y
float32 z
float32 visibility
float32 presence
